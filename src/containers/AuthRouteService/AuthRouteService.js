import React, {PureComponent} from 'react';
import PropTypes from 'prop-types';
import history from '../../history';

class AuthRouteService extends PureComponent {
    render() {
        return (
            <div>

            </div>
        );
    }
}

AuthRouteService.propTypes = {};

export default AuthRouteService;
